# vps-proxy

Infra context.

- Docker, Traefik, reverse proxy, and CI.
- Prefer labels for routing and keep proxy config declarative.
- Keep changes minimal and local.
- Assume shared Docker host deployment and the existing GitHub Actions flow.
